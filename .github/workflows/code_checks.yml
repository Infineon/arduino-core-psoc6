name: Code Checks

on:
  push:
    
jobs:
  makers-devops:
    uses: Infineon/makers-devops/.github/workflows/code_checks.yml@fix-clang-tidy
    with:
      project-yaml: config/project.yml
      user-yaml: config/user.yml
    secrets: inherit

        